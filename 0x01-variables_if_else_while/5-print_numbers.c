#include <stdio.h>
/**
 * Description : main - Prints number of base 10
 * Return: 0 if all its ok.
 */
int main(void)
{

	int b;
		for (b = 0; b <= 9; b++)
			printf("%d", b);
		printf("\n");
		return (0);
}
