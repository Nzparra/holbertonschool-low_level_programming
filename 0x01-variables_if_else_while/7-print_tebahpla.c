#include <stdio.h>
/**
 * Description : main - Reverse alphabet
 * Return: 0 if all its ok.
 */
int main(void)
{
	char l;
		for (l = 'z'; l >= 'a'; --l)
			putchar(l);
		putchar('\n');
		return (0);
}
