#include "holberton.h"
/**
  * _abs - Prints the absolute value of an int.
  * @c: Int that be analized
  * Return: A result of function.
  */
int _abs(int c)
{
	if (c <= 0)
	{
		c = c * -1;
		return (c);
	}
	else
	{
		return (c);
	}
}
